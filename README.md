# 📘 Behave POM BDD Framework  
A scalable, hybrid automation framework using **Behave (BDD)**, **Selenium**, **Page Object Model (POM)**, **Allure Reports**, and optional **BrowserStack** execution.

---

## 🚀 Overview  
This framework provides:

- **Clean BDD syntax** using Behave  
- **Page Object Model (POM)** for maintainable UI tests  
- **Hybrid execution** (Local + BrowserStack)  
- **Dynamic browser/OS selection**  
- **Automatic screenshots on failure**  
- **Allure reporting with step attachments**  
- **Tag-based execution**
- **Parallel Execution Support (BrowserStack)**

---


---

## ⚙️ Installation

```
pip install -r requirements.txt

```
## ⚙️ Run / Execution Commands
``` 
python run_parallel_scenarios.py
behave
```
